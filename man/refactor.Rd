% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refactor.R
\name{refactor}
\alias{refactor}
\title{Refactor flowlines and reconcile catchment divides (end-to-end)}
\usage{
refactor(
  gpkg = NULL,
  flowpaths = NULL,
  divides = NULL,
  events = NULL,
  avoid = NULL,
  split_flines_meters = 10000,
  collapse_flines_meters = 1000,
  collapse_flines_main_meters = 1000,
  min_area_m2 = 800,
  snap_distance_m = 100,
  simplify_tolerance_m = 40,
  term_add = 1000000000L,
  modification_file = NULL,
  fac = NULL,
  fdr = NULL,
  keep = NULL,
  outfile = NULL
)
}
\arguments{
\item{flowpaths}{\code{sf} LINESTRINGs; reference flowline features.}

\item{divides}{\code{sf} POLYGON/MULTIPOLYGON; reference catchment divides.}

\item{events}{Optional POI table (\code{sf} or \code{data.frame}) with at least
\code{event_id} and \code{flowpath_id} (pre-refactor). Used to steer splits and
avoid collapsing immediately downstream of POIs.}

\item{avoid}{Optional integer/numeric vector of flowpath IDs (COMIDs)
to exclude from collapse operations.}

\item{split_flines_meters}{Numeric. Maximum desired segment length in the
refactored network (default \code{10000}).}

\item{collapse_flines_meters}{Numeric. Minimum inter-confluence length to
retain during collapse (default \code{1000}).}

\item{collapse_flines_main_meters}{Numeric. Minimum between-confluence
length for mainstem collapse (default \code{1000}).}

\item{min_area_m2}{Numeric. Minimum polygon area in m² to keep when
splitting divides (default \code{800}).}

\item{snap_distance_m}{Numeric. Snapping distance (meters) for aligning
split polygons to outlets (default \code{100}).}

\item{simplify_tolerance_m}{Numeric. Douglas–Peucker tolerance (meters)
used during divide splitting (default \code{40}).}

\item{fac}{Path to a flow accumulation raster. If supplied together with
\code{fdr}, divide reconciliation is performed.}

\item{fdr}{Path to a flow direction raster. If supplied together with
\code{fac}, divide reconciliation is performed.}

\item{keep}{Proportion of points to retain during \strong{final} geometry
simplification of divides (passed to \code{ms_simplify}). Use \code{NULL} to
skip final simplification (default \code{NULL}).}

\item{outfile}{Path to a GeoPackage to write outputs. Layers written:
\code{"refactored_flowpaths"}, \code{"reconciled_flowpaths"}, \code{"lookup_table"},
\code{"outlets"}, \code{"pois"}, and \code{"refactored_divides"}.}
}
\value{
(Invisibly) the path to \code{outfile} after writing the layers; also
produces temporary files for intermediate refactor/reconcile steps.
}
\description{
Wrapper that:
\enumerate{
\item Normalizes inputs via an internal setup,
\item Calls \code{refactor_flowpaths()} to split/collapse flowlines,
\item Builds a lookup (\code{reconciled_ID} ↔ \code{member_COMID}),
\item Writes refactored flowlines and reconciliation products to \code{outfile},
\item Reconciles and cleans divides against the refactored network using
FDR/FAC (if provided),
\item Emits auxiliary POI/outlet tables for QA and downstream use.
}
}
\details{
This wrapper writes outputs as it progresses. It constructs a reconciliation
table from the intermediate \code{"reconciled_flowpaths"} product to ensure
divides are split/unioned consistently with the refactored network.
If unioning via \code{hydrofab::union_polygons()} fails due to memory constraints,
a safe \code{sf::st_union()} fallback is used.
}
\section{Outputs}{

\describe{
\item{refactored_flowpaths}{Refactored flowline network in EPSG:5070.}
\item{reconciled_flowpaths}{Per-flowline reconciliation geometries (EPSG:5070).}
\item{lookup_table}{Mapping of \code{reconciled_ID} ↔ \code{member_COMID} and base COMIDs.}
\item{outlets}{Event-tied outlets on the refactored network.}
\item{pois}{Original POI/outlet joins from setup.}
\item{refactored_divides}{Unioned, reconciled divides labeled by \code{ID}
with concatenated \code{member_COMID}; a \code{split_error} flag indicates
fallback geometries where splitting failed.}
}
}

\examples{
\dontrun{
refactor(
  flowpaths = fl,
  divides   = dv,
  events    = pois,
  fac = "/path/fac.tif",
  fdr = "/path/fdr.tif",
  outfile = "refactor_out.gpkg"
)
}

}
\seealso{
\itemize{
\item \code{\link{reconcile_divides}} — divide reconciliation engine.
\item \code{\link{clean_divide_geometry}} — post-reconciliation cleaner.
\item \code{refactor_flowpaths} — network refactoring (package internal/external).
}
}
