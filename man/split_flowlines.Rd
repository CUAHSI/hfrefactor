% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_flowlines.R
\name{split_flowlines}
\alias{split_flowlines}
\title{Split flowlines (sequential, flowpath_id-based)}
\usage{
split_flowlines(flines, max_length = NULL, events = NULL, avoid = NA)
}
\arguments{
\item{flines}{sf LINESTRINGs with columns:
\itemize{
\item flowpath_id (coerced to character)
\item one downstream column: flowpath_toid | toCOMID | toid  (kept as-is)
\item LENGTHKM (will be recomputed from geometry)
CRS should be projected in meters for length-based splitting.
}}

\item{max_length}{numeric (meters). If NULL, only event splits apply.}

\item{events}{data.frame of event matches with \strong{per-event measures}:
required: flowpath_id, REACHCODE, FromMeas, ToMeas, REACH_meas
optional: identifier (passed through to output)}

\item{avoid}{vector of flowpath_id to exclude from \emph{length} splitting (events still apply).}
}
\value{
sf with split flowlines; includes event_* fields and recomputed LENGTHKM.
}
\description{
Split flowlines by events and/or a max segment length.
Returns semantic IDs with .part suffix ordered upstream (.1) -> downstream (.N).
}
