% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_divide_geom.R
\name{clean_divide_geometry}
\alias{clean_divide_geometry}
\title{Clean and normalize divide geometries (POLYGON-only, valid, simplified)}
\usage{
clean_divide_geometry(divides, ..., keep = 0.2, flowlines = NULL)
}
\arguments{
\item{divides}{\code{sf} object of divide geometries in any CRS with at least:
\itemize{
\item \code{member_COMID} (identifier used to track completeness),
\item \code{ID} (grouping key used for dissolve/union steps),
and a \code{POLYGON}/\code{MULTIPOLYGON}/\code{GEOMETRYCOLLECTION} geometry column.
}}

\item{...}{(currently unused) reserved for future options.}

\item{keep}{Numeric in (0, 1] or \code{NULL}. Fraction of vertices to retain
during simplification (passed to \code{rmapshaper::ms_simplify(keep=...)}).
Use \code{NULL} to skip simplification. Default: \code{0.2}.}

\item{flowlines}{Optional \code{sf} LINESTRING layer (same features domain) with
column \code{flowpath_id}. When supplied, intersect tests help choose a
“prime” polygon among multiple parts for a given \code{ID}.}
}
\value{
An \code{sf} object in EPSG:5070 with valid \code{POLYGON} geometries:
\itemize{
\item If multiple parts per \code{ID} were present, the result includes the
cleaned geometry with original attributes re-joined and an
\code{areasqkm} column (via \code{hydrofab::add_areasqkm()}).
\item If only single-part polygons were present, returns the input
attributes with cleaned geometry (and drops internal helper fields
like \code{n}, \code{tmpID} if they exist).
}
}
\description{
Standardizes and repairs per-flowpath divide polygons so each
\code{member_COMID} maps to a single, valid \code{POLYGON} in EPSG:5070.
Handles \code{GEOMETRYCOLLECTION} (extracts polygons), \code{MULTIPOLYGON}
(keeps the largest part per \code{member_COMID}), fixes invalid geometry,
optionally dissolves/splits extras by \code{ID}, re-attaches attributes,
and (optionally) simplifies geometry while preserving shapes.

If multiple polygon parts exist for a given divide, the function
selects a primary part (optionally informed by intersecting
\code{flowlines}) and intelligently re-associates smaller orphan parts.
}
\details{
\strong{Workflow:}
\enumerate{
\item Transform to EPSG:5070; tag geometry type.
\item Extract polygons from \code{GEOMETRYCOLLECTION}; pick largest part from
\code{MULTIPOLYGON} per \code{member_COMID}.
\item Snap to grid (tiny jitter) and validate; repair invalid features
with \code{sf::st_make_valid()}.
\item If any \code{member_COMID} has >1 polygon, dissolve by \code{ID}, explode,
optionally mark a primary part using intersects with \code{flowlines},
and reconnect smaller parts to the most plausible main polygon.
\item Optionally simplify with \code{ms_simplify(keep_shapes = TRUE)}.
\item Guarantee no \code{member_COMID} loss; return cleaned geometry and
attributes (plus \code{areasqkm}).
}

\strong{Assumptions:}
\itemize{
\item \code{divides} contains \code{member_COMID} and \code{ID}.
\item When provided, \code{flowlines} contains \code{flowpath_id} and is spatially
compatible (will be transformed internally).
}
}
\section{CRS}{

Output is returned in EPSG:5070.
}

\examples{
\dontrun{
# Minimal usage
clean <- clean_divide_geometry(divides)

# Skip simplification
clean_nosimp <- clean_divide_geometry(divides, keep = NULL)

# Use flowlines to guide "prime" part selection
clean_guided <- clean_divide_geometry(divides, flowlines = fl, keep = 0.3)
}

}
\seealso{
\code{\link[rmapshaper:ms_simplify]{rmapshaper::ms_simplify()}}, \code{\link[rmapshaper:ms_dissolve]{rmapshaper::ms_dissolve()}},
\code{\link[rmapshaper:ms_explode]{rmapshaper::ms_explode()}}, \code{\link[lwgeom:st_snap_to_grid]{lwgeom::st_snap_to_grid()}},
\code{\link[sf:valid]{sf::st_make_valid()}}, \code{\link[sf:valid]{sf::st_is_valid()}}
}
