% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_divides.R
\name{split_divide}
\alias{split_divide}
\title{Split a catchment divide into upstream/downstream pieces using FDR/FAC}
\usage{
split_divide(
  feature_id,
  to_split_cat,
  to_split_fline,
  fdr,
  fac,
  min_area_m2,
  snap_distance_m,
  simplify_tolerance_m
)
}
\arguments{
\item{feature_id}{Integer-like. Base FEATUREID of the catchment being split;
used only for logging/error messages.}

\item{to_split_cat}{\code{sf} POLYGON/MULTIPOLYGON (EPSG:5070 preferred). The
catchment/divide geometry to be split.}

\item{to_split_fline}{\code{sf} LINESTRING(s) crossing \code{to_split_cat}. The line
endpoints (computed via \code{lwgeom::st_endpoint()}) are used as ordered
outlets for successive splits.}

\item{fdr}{A flow-direction raster (or a path readable by \code{terra::rast()}),
already cropped/projected to a neighborhood that covers \code{to_split_cat}.}

\item{fac}{A flow-accumulation raster (or a path readable by \code{terra::rast()}),
co-registered with \code{fdr}. Must have identical dimensions after prep.}

\item{min_area_m2}{Numeric. Minimum polygon area in \strong{m²} to retain when
extracting the downstream remainder (filters slivers).}

\item{snap_distance_m}{Numeric. Distance (meters) used to snap the
upstream piece to the outlet point to ensure topological contact.}

\item{simplify_tolerance_m}{Numeric. Douglas–Peucker tolerance (meters)
used to simplify the upstream polygon before differencing.}
}
\value{
An \code{sfc} (EPSG:5070) of polygonal geometries in order:
all \strong{upstream} pieces for each intermediate outlet, followed by the
\strong{downstream} remainder as the last element. Each element is a valid
polygonal geometry; overall the vector may contain \code{POLYGON} or
\code{MULTIPOLYGON} members depending on topology.
}
\description{
Given a single catchment polygon (\code{to_split_cat}) and one or more refactored
flowline segments traversing it (\code{to_split_fline}), this function iteratively
carves the divide into \strong{upstream} pieces for each intermediate outlet and a
final \strong{downstream remainder}. It uses a pre-extracted flow-direction (FDR)
and flow-accumulation (FAC) raster window aligned to the catchment to
delineate contributing-area masks, then converts those masks to polygons and
performs geometric difference operations to avoid overlaps.
}
\details{
\strong{Algorithm sketch}
\enumerate{
\item Compute outlet coordinates from \code{to_split_fline} endpoints and verify
each falls within \code{to_split_cat}.
\item Prepare aligned FDR/FAC matrices via
\code{hydrofab:::prep_cat_fdr_fac(to_split_cat, fdr, fac)}.
\item For each outlet (except the terminal one), locate its raster cell
(\code{hydrofab:::get_row_col()}), collect all upstream cells
(\code{hydrofab:::collect_upstream()}), and rasterize a binary mask.
\item Convert the mask to polygons, simplify (\code{simplify_tolerance_m}),
snap to the outlet (\code{snap_distance_m}), and make valid.
\item Difference the current \code{catchment} by the upstream piece to obtain
a downstream remainder; drop tiny remnants \verb{< min_area_m2}, and keep
the largest polygonic remainder.
\item Append the upstream piece to the output list, update \code{catchment} to
the remainder, and continue to the next outlet.
\item After all outlets are processed, append the final remainder.
}

If at any step an outlet does not lie within the catchment, the function
logs an error and stops. A consistency check ensures FDR/FAC matrices share
identical dimensions.
}
\examples{
\dontrun{
pieces <- split_divide(
  feature_id = 1001,
  to_split_cat = cat_sf,      # single catchment polygon
  to_split_fline = flines_sf, # refactored segment(s) inside the catchment
  fdr = fdr_rast,             # terra SpatRaster or path
  fac = fac_rast,             # terra SpatRaster or path
  min_area_m2 = 2e4,
  snap_distance_m = 30,
  simplify_tolerance_m = 5
)
}

}
\seealso{
Non-exported helpers used internally:
\code{hydrofab:::prep_cat_fdr_fac},
\code{hydrofab:::get_row_col},
\code{hydrofab:::collect_upstream}.
}
