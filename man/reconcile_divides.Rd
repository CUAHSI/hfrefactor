% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_divides.R
\name{reconcile_divides}
\alias{reconcile_divides}
\title{Reconcile refactored flowpaths with catchment divide polygons}
\usage{
reconcile_divides(
  divides,
  refactored_fp,
  reconciled_fp,
  fdr,
  fac,
  dem_vrt,
  min_area_m2,
  snap_distance_m,
  simplify_tolerance_m,
  keep
)
}
\arguments{
\item{divides}{\code{sf} polygon layer of original catchment divides; must contain
\code{FEATUREID} (numeric or numeric-like character). Geometry can be any
polygonal type; will be transformed/cast as needed.}

\item{refactored_fp}{\code{sf} line layer of \strong{refactored} flowpaths with a
\code{COMID} column (character). Dotted \code{COMID} values (e.g., \code{"1001.2"})
indicate splits of a base feature id (\code{1001}).}

\item{reconciled_fp}{\code{sf} (any geometry) containing the mapping between
\code{ID} (reconciled divide identifier) and \code{member_COMID} (comma-separated
list of member flowpath COMIDs). Only the columns \code{ID} and \code{member_COMID}
are used.}

\item{fdr}{Flow direction raster \strong{or} a path readable by
\code{\link[terra:rast]{terra::rast()}}. CRS may differ; it is reprojected/cropped internally.}

\item{fac}{Flow accumulation raster \strong{or} a path readable by
\code{\link[terra:rast]{terra::rast()}}. CRS may differ; it is reprojected/cropped internally.}

\item{dem_vrt}{Optional path to a DEM VRT/TIFF used when computing FDR/FAC
on the fly (WhiteboxTools). In the current configuration (\code{use_dem = FALSE})
this is not used; kept for parity and future toggling.}

\item{snap_distance_m}{Numeric. Snapping distance (meters) used by the
splitting helper when aligning boundaries to flowlines/rasters.}

\item{simplify_tolerance_m}{Numeric. Douglas–Peucker tolerance (meters)
applied by the splitting helper for output smoothing.}

\item{keep}{Numeric in (0,1] or \code{NULL}. Intended fraction of vertices to
retain for a final simplification pass. \strong{Currently unused} in this
function; reserved for future refinement to mirror other cleaners.}

\item{min_area_m}{Numeric. Minimum polygon area (m²) to retain during split
operations (passed through to the splitting helper).}
}
\value{
An \code{sf} MULTIPOLYGON layer in EPSG:5070 with columns:
\itemize{
\item \code{ID} — reconciled divide identifier (from \code{reconciled_fp})
\item \code{member_COMID} — comma-separated COMIDs associated to \code{ID}
\item \code{geometry} — reconciled divide geometry (MULTIPOLYGON)
}
The result contains:
\enumerate{
\item divides that required splitting (derived via FDR/FAC),
\item divides that did not require splitting (passed through),
\item divides that were unioned where a single \code{ID} has multiple members.
}
}
\description{
Produces a reconciled set of catchment \emph{divides} (MULTIPOLYGON, EPSG:5070)
consistent with a refactored flowpath network. Flowpaths that were split
during refactoring (e.g., have dotted \code{COMID} such as \code{"12345.1"}) trigger
geometric subdivision of their parent divide using flow-direction (FDR) and
flow-accumulation (FAC) rasters. Unsplit divides are passed through.
When multiple refactored flowpaths map to one reconciled ID, the function
unions the corresponding pieces. A log of per-FEATUREID outcomes is written
under \verb{/tmp/reconcile_divides/}.
}
\details{
\strong{High-level workflow}
\enumerate{
\item Normalize \code{reconciled_fp} to a distinct mapping of \code{ID} → \code{member_COMID}.
\item Keep only refactored flowpaths whose base \code{COMID} exists in \code{divides}.
\item Identify base FEATUREIDs that were split (COMIDs like \code{"x.y"}).
\item For each such FEATUREID, crop/mask FDR/FAC (or compute from \code{dem_vrt}
using WhiteboxTools if toggled), then invoke a splitting helper
(\code{split_divide()}, falling back to \code{hydrofab:::split_catchment_divide()}).
\item Cast to \code{MULTIPOLYGON} and de-duplicate per FEATUREID.
\item Collect divides that needed no split and still match refactored flowpaths.
\item Where \code{reconciled_fp} maps multiple \code{member_COMID} to one \code{ID},
union split pieces via \code{hydrofab:::get_split_cats()}.
\item Join back to \code{reconciled_fp} so output carries both \code{ID} and \code{member_COMID}.
}

\strong{Logging}
A TSV log is appended at a timestamped path under
\verb{/tmp/reconcile_divides/}. Each row captures start time, status,
FEATUREID, DEM source hint (NED vs GEDTM30), and whether a retry path
was used. This is helpful for post-hoc QA of problematic basins.

\strong{Assumptions & Notes}
\itemize{
\item \code{FEATUREID} in \code{divides} corresponds to the \strong{base} of dotted \code{COMID}
values in \code{refactored_fp} (e.g., \code{FEATUREID == 1001} matches \code{"1001.1"}).
\item Rasters may be reprojected/cropped/masked internally to a buffered area
around the target divide.
\item This function references non-exported helpers in \strong{hydrofab} via
\verb{hydrofab:::} as a fallback path; these are expected to be available
when running inside the package context.
}
}
\examples{
\dontrun{
out <- reconcile_divides(
  divides = divides_sf,
  refactored_fp = refactored_flowlines_sf,
  reconciled_fp = id_to_members_sf,  # columns: ID, member_COMID
  fdr = "/path/to/fdr.tif",
  fac = "/path/to/fac.tif",
  dem_vrt = "/path/to/dem.vrt",      # not used unless DEM path is enabled
  min_area_m = 2e4,
  snap_distance_m = 30,
  simplify_tolerance_m = 5,
  keep = NULL
)
}

}
\seealso{
\itemize{
\item \code{\link{split_divide}} — primary splitting helper used here.
\item Non-exported fallbacks: \code{hydrofab:::split_catchment_divide},
\code{hydrofab:::get_split_cats}.
\item Raster ops: \code{\link[terra:rast]{terra::rast()}}, \code{\link[terra:crop]{terra::crop()}}, \code{\link[terra:mask]{terra::mask()}}.
}
}
